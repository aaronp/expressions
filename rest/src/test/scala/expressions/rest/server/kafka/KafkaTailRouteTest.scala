package expressions.rest.server.kafka

import expressions.rest.server.BaseRouteTest

class KafkaTailRouteTest extends BaseRouteTest {

  "KafkaTailRoute" should {
    "work" in {
      // TODO - there's an open SO question, but it'd be dandy to test the websocket routes in a unit test
      // we can settle for testing the underlying stream logic at least
    }
  }
}
